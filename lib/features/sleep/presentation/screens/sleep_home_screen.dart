import 'package:flutter/material.dart';

class SleepHomeScreen extends StatelessWidget {
  const SleepHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Sleep Home')),
    );
  }
}
